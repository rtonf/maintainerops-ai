# Contributing

MaintainerOps AI accepts small, reviewable changes.

Before opening a pull request:

```bash
npm install
npm run build
npm test
npm run eval
```

Prefer changes that preserve the human-in-the-loop workflow. Features that automatically merge, close, publish, or scan unauthorized targets should not be added.
