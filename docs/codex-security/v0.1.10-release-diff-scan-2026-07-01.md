# v0.1.10 Release Security Diff Review: 2026-07-01

## Scope

Reviewed the `v0.1.10` release-candidate diff before publication:

- npm Trusted Publishing workflow addition.
- Source version bump to `0.1.10`.
- Model-backed eval selected-case expansion.
- Label normalization safeguards for release-readiness and actionable security issues.
- Public release, README, eval, and maintenance documentation updates.

## Security-Relevant Observations

- The npm publish workflow uses GitHub Actions OIDC (`id-token: write`) and does not require or reference `NPM_TOKEN`.
- The publish workflow is not triggered by pull requests.
- The publish workflow verifies the package with `npm run verify` before `npm publish`.
- Model-backed eval execution remains manual-only and is not added to CI.
- Live evals remain guarded by explicit local `OPENAI_API_KEY` presence and bounded by `--budget-usd`, `--max-cases`, and `--max-output-tokens`.
- Label normalization changes do not perform network, filesystem write, command execution, or GitHub mutation operations.
- Feedback-request issues are still prevented from receiving noisy security or release labels when low risk.

## Findings

No reportable security finding was identified in this release-candidate diff.

## Follow-Up

- After merge, confirm branch protection and CodeQL pass on `main`.
- After publishing `v0.1.10`, verify the Trusted Publishing workflow run and npm package provenance evidence.
- Keep a full Codex Security repository-wide rescan for the next larger runtime/security-sensitive change set.
