# v0.1.14 Consistency Diff Review: 2026-07-07

This API-free security diff review covers the `v0.1.14` Marketplace snapshot and package metadata consistency release.

## Scope

- `package.json` and `package-lock.json` version bump to `0.1.14`.
- npm package repository URL normalization.
- README GitHub Action example tag refresh from stale `v0.1.12` to the verified Marketplace latest `v0.1.13`.
- Release evidence documentation for `v0.1.14`.

## Security Boundary Review

- No runtime code paths changed.
- No new GitHub permissions were added.
- No OpenAI API behavior changed.
- No repository mutation behavior was added.
- No auto-merge, auto-close, auto-label, comment-posting, release-publishing, or unauthorized scanning behavior was added.
- No secret-handling, redaction, prompt construction, model-output parsing, or GitHub API request logic changed.

## Result

No reportable security findings.

## Verification

- `npm run verify`
- `git diff --check`
- npm Trusted Publishing run `28830553903` completed successfully after release.
- `npm view maintainerops-ai version dist-tags time --json` reported `0.1.14` as `latest`.
- Public `npm exec --yes --package maintainerops-ai@latest -- maintainerops demo --format markdown` printed the no-key review packet.
- GitHub Marketplace public listing check confirmed `v0.1.14` as `Latest`.

The release introduced no new runtime or security-sensitive behavior beyond package metadata and public evidence documentation.
