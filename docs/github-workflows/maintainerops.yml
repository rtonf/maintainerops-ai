name: MaintainerOps AI

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  issues:
    types: [opened, edited, reopened]

permissions:
  contents: read
  issues: read
  pull-requests: read

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - run: npm ci
      - run: npm run build
      - name: Analyze pull request
        if: github.event_name == 'pull_request'
        run: node dist/cli.js analyze --repo "$GITHUB_REPOSITORY" --pull "${{ github.event.pull_request.number }}" --format markdown --offline --authorized
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Analyze issue
        if: github.event_name == 'issues'
        run: node dist/cli.js analyze --repo "$GITHUB_REPOSITORY" --issue "${{ github.event.issue.number }}" --format markdown --authorized
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
