name: MaintainerOps AI

on:
  pull_request_target:
    types: [opened, synchronize, reopened, ready_for_review]
  issues:
    types: [opened, edited, reopened]
  workflow_dispatch:

permissions:
  contents: read
  issues: read
  pull-requests: read

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - name: Analyze pull request
        if: github.event_name == 'pull_request_target'
        uses: rtonf/maintainerops-ai@2752d81196935bb361e13cfd8877905151143698
        with:
          mode: pull_request
          repo: ${{ github.repository }}
          number: ${{ github.event.pull_request.number }}
          format: markdown
          offline: true
          authorized: true
      - name: Analyze issue
        if: github.event_name == 'issues'
        uses: rtonf/maintainerops-ai@2752d81196935bb361e13cfd8877905151143698
        with:
          mode: issue
          repo: ${{ github.repository }}
          number: ${{ github.event.issue.number }}
          format: markdown
          offline: true
          authorized: true
